<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.codefx.demo</groupId>
	<artifactId>junit-5</artifactId>
	<version>1.0-SNAPSHOT</version>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<junit5-version>5.0.0-ALPHA</junit5-version>
	</properties>

	<build>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.1</version>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<dependencies>
		<!-- this is all you need to write tests with JUnit 5 -->
		<dependency>
			<groupId>org.junit</groupId>
			<artifactId>junit5-api</artifactId>
			<version>${junit5-version}</version>
			<scope>test</scope>
		</dependency>

		<!-- to execute v5-tests as part of a v4-run, we need these two artifacts -->
		<dependency>
			<!-- contains the engine that actually runs the v5-tests -->
			<groupId>org.junit</groupId>
			<artifactId>junit5-engine</artifactId>
			<version>${junit5-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<!-- contains the runner that adapts v5-tests for a v4-run -->
			<groupId>org.junit</groupId>
			<artifactId>junit4-runner</artifactId>
			<version>${junit5-version}</version>
			<scope>test</scope>
		</dependency>

		<!-- other test libraries work out of the box. e.g. ... -->
		<dependency>
			<!-- ... AssertJ ... -->
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<version>3.3.0</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<!-- ... or Mockito ... -->
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<version>1.10.19</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

</project>